<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:CatFacts.ViewModels"
             x:Class="CatFacts.Views.BreedPage"
             Title="Cat Breeds"
             BackgroundImageSource="breedsbackground.jpg"
             NavigationPage.HasNavigationBar="False">
    <Grid Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Text="Get New Breed"
                Grid.Row="0"
                BackgroundColor="#6200EE"
                WidthRequest="150"
                TextColor="White"
                FontSize="16"
                Padding="10"
                CornerRadius="10"
                HorizontalOptions="Center"
                VerticalOptions="Start"
                Margin="0,10,0,10"
                Command="{Binding GetBreedCommand}"
                IsEnabled="True" />

        <Button Text="Go to Cat Facts"
                Grid.Row="1"
                BackgroundColor="#03DAC6"
                WidthRequest="150"
                TextColor="White"
                FontSize="16"
                Padding="10"
                CornerRadius="10"
                HorizontalOptions="Center"
                VerticalOptions="Start"
                Margin="0,0,0,20"
                Command="{Binding NavigateToCatFactsCommand}"
                IsEnabled="True" />

        <CollectionView Grid.Row="2"
                        ItemsSource="{Binding Breeds}"
                        SelectionMode="None">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="3" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BackgroundColor="Orange"
                           Padding="10"
                           CornerRadius="10"
                           HasShadow="True">
                        <StackLayout>
                            <Label Text="{Binding BreedName, StringFormat='Breed: {0}'}"
                                   FontSize="16"
                                   TextColor="Black"
                                   FontAttributes="Bold"
                                   LineBreakMode="WordWrap" />
                            <Label Text="{Binding Country, StringFormat='Country: {0}'}"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   TextColor="White" />
                            <Label Text="{Binding Origin, StringFormat='Origin: {0}'}"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   TextColor="White" />
                            <Label Text="{Binding Coat, StringFormat='Coat: {0}'}"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   TextColor="White" />
                            <Label Text="{Binding Pattern, StringFormat='Pattern: {0}'}"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   TextColor="White" />
                            <Button Text="Eliminar"
                                    BackgroundColor="Red"
                                    TextColor="White"
                                    WidthRequest="200"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:BreedViewModel}}, Path=DeleteBreedCommand}"
                                    CommandParameter="{Binding .}"
                                    IsEnabled="True" />
                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>